package com.tipwheal.el;

public abstract class Player {
	/**
	 * play is an abstract method in Player
	 * 
	 * @param info
	 *            GameInfo
	 * @return change GameInfo and return
	 */
	public abstract GameInfo play(GameInfo info);
}
